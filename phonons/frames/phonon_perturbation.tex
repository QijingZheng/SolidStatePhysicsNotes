\begin{frame}[allowframebreaks]
  \frametitle{Periodic Displacement of Ions --- Phonon Perturbation}

  In a solid, the external potential can be expressed as the superposition of
  atomic potential $v_s(\boldr)$
  \begin{equation*}
    V_{ext}(\boldr) = \sum_{j}\sum_{s}^{N_a} v_s(\boldr - \bdm{x}_{j} -
    \bdm{d}_s - \bdm{u}^j_s)
  \end{equation*}

  Consider the periodic displacement of the ions of the form
  \begin{equation}
    \label{eq:phonon_periodic_perturb}
    u_{s\alpha}^j(\bdm{q}) = \mu_{s\alpha}  \, e^{ i\bdm{q}\cdot \bdm{x}_j}
                           + \mu_{s\alpha}^*\, e^{-i\bdm{q}\cdot \bdm{x}_j}
    \qquad
    (\alpha = x, y, z)
  \end{equation}
  \begin{center}
    \resizebox{0.6\textwidth}{!}{
      \begin{tikzpicture}
      \foreach \i in {0, 1,...,6}{
          \foreach \j in {0, 1}{
          \foreach \x/\y/\c in {0.3/0.3/blue, 0.7/0.7/blue, 0.2/0.8/red, 0.8/0.2/red}{
              \shade[ball color=\c] (\x + \i, \y + \j) circle (0.08);
          }
          }
      }
      \draw[help lines, thin, draw=gray] (0, 0) grid (7, 2);

      \draw[line width=0.8pt, color=ForestGreen, domain=0:7.0, densely dotted] plot[samples=300]
      (
      \x, {0.3 + 0.3 * cos((\x - 0.3) * 2. * pi / 7 r)}
      );
      \foreach \x in {0,1,...,6}{
        \pgfmathparse{0.3 * cos((\x) * 2 * pi / 7. r)}
        \let \y = \pgfmathresult
        \shade[] (\x +0.3, 0.3 + \y) circle (0.05);
        \draw[-{Stealth[length=6pt, open]}, tips=proper, thick, ForestGreen] (\x+0.3, 0.3) -- ++(0.0, \y);
      };

      \draw[line width=0.8pt, color=ForestGreen, domain=0:7.0, densely dotted] plot[samples=300]
      (
      \x, {1.3 + 0.3 * cos((\x - 0.3) * 2. * pi / 7 r)}
      );
      \foreach \x in {0,1,...,6}{
        \pgfmathparse{0.3 * cos((\x) * 2 * pi / 7. r)}
        \let \y = \pgfmathresult
        \shade[] (\x +0.3, 1.3 + \y) circle (0.05);
        \draw[-{Stealth[length=6pt, open]}, tips=proper, thick, ForestGreen] (\x+0.3, 1.3) -- ++(0.0, \y);
      };
      \end{tikzpicture}
    }
  \end{center}
  \begin{align*}
    \frac{
      \partial V^{\bdm{q}}_{ext}(\boldr)
    }{
      \partial \mu_{s\alpha}
    }
    &=
    -\sum_j
    e^{i\bdm{q}\cdot\bdm{x}_j}\,
    \left.
    \frac{
      \partial v_{s}(\boldr - \bdm{x}_j - \bdm{d}_s)
    }{
      \partial r_\alpha
    }
    \right|_{u_s^j=0} \\
    &=
    -e^{i\bdm{q}\cdot\bdm{r}}\,
    \mathcolor{magenta}{
    \sum_j
    e^{i\bdm{q}\cdot{(\bdm{x}_j - \boldr)}}\,
    \left.
    \frac{
      \partial v_{s}(\boldr - \bdm{x}_j - \bdm{d}_s)
    }{
      \partial r_\alpha
    }
    \right|_{u_s^j=0}
    }
  \end{align*}

  The \textcolor{magenta}{colored quantity} has the periodicity of the lattice.

  \break

  The dynamical matrix 
  \begin{equation*}
    D_{s\alpha, t\beta}(\bdm{q})
    =
    {1\over\sqrt{M_sM_t}}
    \sum_k
    e^{-i\bdm{q}\cdot\bdm{x}_j}
    \frac{
      \partial^2 E^0_{\text{tot}}
    }{
      \partial {u}_{s\alpha}^j
      \partial {u}_{t\beta}^k
    }
    e^{i\bdm{q}\cdot\bdm{x}_k}
    =
    {N\over\sqrt{M_sM_t}}
    \frac{
      \partial^2 E^0_{\text{tot}}
    }{
      \partial {\mu}_{s\alpha}^*
      \partial {\mu}_{t\beta}
    }
  \end{equation*}
  Inserting the equation for the second derivative Eq.~\ref{eq:energy_2nd_deriv}
  \begin{align*}
    D_{s\alpha, t\beta}(\bdm{q})
    &=
    {N\over\sqrt{M_sM_t}}
    \int_V\,
    \mathcolor{red}{
    \sum_k
    e^{-i\bdm{q}\cdot\bdm{x}_j}
    \frac{
      \partial^2 V_{ext}^{\bdm{q}}(\boldr)
    }{
      \partial {u}_{s\alpha}^j
      \partial {u}_{t\beta}^k
    }\,
    \rho(\boldr)
    \, e^{i\bdm{q}\cdot\bdm{x}_k}
    }
    \,\dt{\boldr} \\[3pt]
    &+
    {1\over\sqrt{M_sM_t}}
    \int_V\,
    \mathcolor{blue}{
    \sum_k
    e^{-i\bdm{q}\cdot\bdm{x}_j}
    \frac{
      \partial V_{ext}^{\bdm{q}}(\boldr)
    }{
      \partial {u}_{s\alpha}^j
    }
    \frac{
      \partial \rho(\boldr)
    }{
      \partial {u}_{t\beta}^k
    }
    \, e^{i\bdm{q}\cdot\bdm{x}_k}
    }
    \,\dt{\boldr} \\[3pt]
  \end{align*}


\end{frame}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
